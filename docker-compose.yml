services:
  telecat-frontend:
    container_name: telecat-frontend
    image: telecat-frontend:latest
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env.production
    ports:
      - "2616:2616"
